@model List<Category> 

<div class="widget">
                        <h2 class="section-title mb-3">Kategoriler</h2>
                        <div class="widget-body">
                            <ul class="widget-list">
                                @foreach(var c in Model)
                                 {
                                          <li><a href="#!">@c.Name <span class="ml-auto">(3)</span></a></li> 
                                  }
                                
                            </ul>
                        </div>
                    </div>